## The indent is to have a properly formatted rule
    ## Add parameter with comma except for the last element in parameters
    insert(new ${class}(#foreach(${param} in ${parameters})${param}#if($foreach.hasNext), #end#end));